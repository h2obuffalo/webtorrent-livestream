# WebTorrent Live Streaming Environment Variables

# Server Configuration
NODE_ENV=development
PORT=3000

# Owncast Configuration
OWNCAST_DATA_PATH=../owncast/data/hls/0
OWNCAST_API_URL=http://localhost:8080
OWNCAST_ADMIN_PASSWORD=

# Broadcaster Configuration
HTTP_PORT=3000
CHUNK_RETENTION_MINUTES=5

# Signaling Server Configuration
WS_PORT=8080
SIGNALING_URL=ws://localhost:8080

# Cloudflare R2 Configuration
CDN_HOSTNAME=tv.danpage.uk
CLOUDFLARE_API_KEY=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=bangbucket
CLOUDFLARE_ACCOUNT_ID=

# WebTorrent Configuration
TRACKER_URLS=wss://tracker.openwebtorrent.com,wss://tracker.webtorrent.dev
WEBTORRENT_ANNOUNCE=wss://tracker.openwebtorrent.com,wss://tracker.webtorrent.dev

# R2 Upload Path
R2_UPLOAD_PATH=live/

# Cache Configuration (in seconds)
CACHE_MANIFEST=30
CACHE_CHUNKS=300

# Monitoring
ENABLE_DEBUG_LOGGING=false
METRICS_PORT=9090

# Deployment (AWS EC2)
AWS_REGION=us-east-1
EC2_INSTANCE_TYPE=t3.medium

# Setup Instructions:
# 1. Copy this file to .env and fill in your actual credentials
# 2. Install and configure Owncast (https://owncast.online)
# 3. Update OWNCAST_DATA_PATH to point to your Owncast HLS output directory
# 4. Create Cloudflare R2 bucket and add credentials
# 5. Start services: npm run dev

